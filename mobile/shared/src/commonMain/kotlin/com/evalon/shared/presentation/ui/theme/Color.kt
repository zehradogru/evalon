package com.evalon.shared.presentation.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Evalon Design System — Color Palette ───────────────────────────────────

// Backgrounds
val EvalonBlack = Color(0xFF000000)
val EvalonDarkBg = Color(0xFF0F0F0F)
val EvalonDarkSurface = Color(0xFF0F0F0F)
val EvalonSurfaceVariant = Color(0xFF2E2E2E)

// Accent / Brand
val EvalonBlue = Color(0xFF2862FF)
val EvalonOrange = Color(0xFFEF9005)

// Semantic
val EvalonGreen = Color(0xFF24A693)
val EvalonRed = Color(0xFFF23645)

// Text
val EvalonTextPrimary = Color(0xFFFFFFFF)
val EvalonTextSecondary = Color(0xFF8B8B9E)

// Gradients (derived from accent)
val EvalonBlueDark = Color(0xFF1E4FCC)
val EvalonGreenDark = Color(0xFF1C8A7A)
val EvalonRedDark = Color(0xFFD42E3C)
