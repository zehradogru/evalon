'use client'

import { SimulatorShell } from './simulator-shell'

/**
 * Paper Trade Dashboard — now renders the Trading Simulator directly.
 */
export function PaperTradeDashboard() {
    return <SimulatorShell />
}
