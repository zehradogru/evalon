{
    "name": "@graph/chart-client",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "@graph/shared-types": "workspace:*",
        "@graph/chart-utils": "workspace:*",
        "lightweight-charts": "^4.2.0"
    },
    "devDependencies": {
        "typescript": "^5.7.0",
        "vite": "^6.1.0"
    }
}